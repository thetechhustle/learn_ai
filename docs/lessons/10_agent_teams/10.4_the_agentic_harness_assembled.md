### 10.4 The Agentic Harness, Assembled

Step back and look at what's accumulated in your repo: CLAUDE.md and a second brain, Skills, connections, hooks, a validation harness, permission policy, defined agent roles, team templates. Separately, each was a lesson. Together they're a *thing with a name* — the **agentic harness**: the complete environment that makes agent work on this project reliable by construction. This lesson assembles the view, finds your gaps, and packages the harness so it travels.

!!! abstract "What you will learn"
    - See the harness as one system: what each layer contributes and how they interlock.
    - Audit any project's harness for gaps with the layer checklist.
    - Bootstrap the full harness onto a new project in under an hour.

!!! success "Builder principle"
    The model is rented; the harness is owned. Everything you've built since Chapter 5 is equity — this lesson is where you count it.

## The full stack, one diagram

```
            THE AGENTIC HARNESS  (all of it: files in your repo)
┌─────────────────────────────────────────────────────────────┐
│ KNOWLEDGE   CLAUDE.md · second brain · memory layers   (Ch.5)│  what's true here
│ CAPABILITY  Skills · commands                        (Ch.6-7)│  how we do things
│ REACH       MCP servers · CLIs · connection postures   (Ch.8)│  what we can touch
│ VALIDATION  harness checks · hooks at the seams        (Ch.9)│  what good means, enforced
│ POLICY      allow/ask/deny · trust boundaries      (Ch.8-9)  │  what's permitted
│ CREW        agent roles · team templates · contracts  (Ch.10)│  who does what
└─────────────────────────────────────────────────────────────┘
                            ▲
        any agent, any session, any lane inherits ALL of it
```

The property that makes this an *architecture* rather than a pile: **every layer is files in the repo.** Which yields the three payoffs you've been collecting without totaling:

1. **Consistency** — every session, subagent, and worktree lane runs inside the same machine. The 10.2 "nothing to configure" magic was this.
2. **Portability** — clone the repo, get the harness. A collaborator (or *you*, on a new laptop, or a teammate's agent) inherits months of ratcheted judgment in one `git clone`.
3. **Compoundability** — every correction you ratcheted (5.2), every check you added (9.3), every contract you wrote (10.3) made *all future work* better. Linear effort, compounding return. That's the definition of an asset.

## The audit

Grade any project — including today, yours — one question per layer:

| Layer | The question | Red flag |
| --- | --- | --- |
| Knowledge | Would a fresh session know what's true here without being told? | You're re-explaining in briefs |
| Capability | Are the recurring processes packaged and firing? | Same corrections, different sessions |
| Reach | Does the agent touch what the work actually needs — no more? | You're the courier again (8.1) — or ALLOW sprawl |
| Validation | Can the agent catch its own defects in-loop? | "Done!" without receipts |
| Policy | Is your attention spent only on sharp gates? | Rubber-stamping — or a DENY list still in your head |
| Crew | Can work parallelize *today* if the task warrants? | Every job runs single-lane by default |

Score honestly: most projects are strong in two layers and hollow in two — usually whichever chapters were skimmed. The audit output is a punch list, and the punch list is this lesson's exercise.

## Bootstrapping: the harness in an hour

The real test of owning a system is standing it up *fresh*. New project, empty folder — the bootstrap sequence, and note that every step is a lesson you've done:

```
0-10 min   git init, README, CLAUDE.md v1 (5.2's five sections)
10-20      second-brain skeleton: docs/decisions.md, log.md (5.4)
20-30      harness tier 1 + the after-edit and session-end hooks (9.2-9.3)
30-40      policy: your standard three bands, copied from your last
           project and trimmed to this one (9.4)
40-50      connections this project needs, three-step check each (8.3)
50-60      crew: reviewer role in, team templates copied (10.1, 10.3)
```

Better: you have an agent, and this is a *process with a right way* — which is the 7.1 definition of Skill material. Package the bootstrap as `project-init`, your most meta Skill, and the hour becomes ten minutes of review. Your harness now *reproduces itself*. (The course's own repo was bootstrapped this way. Turtles all the way down.)

=== "🌱 No-code lane"

    Count your equity for real: open your main project and walk the diagram — you have every layer, built by hand, understood from the inside. Two moves this week: run the audit and fix your weakest layer (you already know which one you skimmed); then bootstrap a *fresh* project with the hour sequence, feeling how much of "starting from scratch" you've permanently deleted. That feeling has a market rate, by the way — Chapter 11 is next.

=== "⚙️ Engineer lane"

    This assembled view is what the industry's premium training actually sells — you've built it lesson by lesson instead. The work-relevant frame: **the harness is your team's agentic platform**, and platform thinking applies — version it (it's files; tag it), template it (an org-level starter harness repo beats N divergent ones), measure it (the audit as a recurring team ritual; red flags are backlog items), and staff it (harness improvements are infrastructure work — they deserve tickets, review, and credit, because they multiply everyone's agents). The team whose harness compounds fastest simply outships the one collecting prompt tips. That's the strategic bet, stated plainly.

## Try it now

1. Run the six-question audit on your main project. Write the scores in docs/log.md — a dated harness health check, the first of many.
2. Fix the weakest layer this week (you have the exact lesson number to revisit — that's what the diagram's chapter tags are for).
3. Bootstrap a fresh throwaway project with the hour sequence, then package it: the `project-init` Skill, tested 7.3-style.
4. Journal the equity statement, one paragraph: what does your harness know, enforce, and reach *that no fresh install of any tool has?* That paragraph is your moat (7.4), itemized.

## Check your understanding

- Name the six layers and each one's contribution, from memory.
- Why does "every layer is files" produce consistency, portability, and compounding — mechanically, each?
- What's your project's weakest layer per the audit, and which chapter rebuilds it?
- Why is `project-init` the "most meta" Skill?
